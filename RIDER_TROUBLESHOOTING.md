# Rider Troubleshooting Guide

Руководство по решению проблем с видимостью файлов в JetBrains Rider.

---

## Проблема: Rider не видит файлы документации

### ✅ Файлы присутствуют в git

Все файлы правильно закоммичены и находятся в репозитории:

```bash
# Проверить можно командой:
./open-docs.sh

# Или вручную:
git ls-files | grep -E "(README|Docs)"
```

---

## 🔧 Решения

### Решение 1: Обновить Git Cache

**В Rider:**

1. Откройте меню: `VCS → Git → Refresh`
2. Или используйте горячую клавишу:
   - **Windows/Linux**: `Ctrl+Alt+Y`
   - **Mac**: `Cmd+Option+Y`

3. В появившемся окне нажмите **OK**
4. Если не помогло, попробуйте **Force Refresh**

---

### Решение 2: Синхронизировать файловую систему

**В Rider:**

1. В **Project Explorer** → ПКМ на корне проекта
2. Выберите **Reload from Disk**
3. Или: `File → Synchronize`
4. Горячая клавиша: `Ctrl+Alt+Y` (Win) / `Cmd+Option+Y` (Mac)

---

### Решение 3: Проверить текущую ветку

Убедитесь, что Rider находится на правильной ветке:

**Текущая ветка проекта:**
```
claude/audit-project-migration-01HFuENs9TnbYiKa93AZWBA7
```

**В Rider:**
1. Внизу справа → Git branch indicator
2. Убедитесь, что выбрана ветка: `claude/audit-project-migration-01HFuENs9TnbYiKa93AZWBA7`
3. Если нет → переключитесь на эту ветку

---

### Решение 4: Очистить кэш Rider

**Если предыдущие решения не помогли:**

1. Закройте Rider
2. Удалите кэш:
   - **Windows**: `%LOCALAPPDATA%\JetBrains\Rider[Version]\caches`
   - **Mac**: `~/Library/Caches/JetBrains/Rider[Version]`
   - **Linux**: `~/.cache/JetBrains/Rider[Version]`

3. Откройте Rider снова
4. Дождитесь переиндексации проекта

---

### Решение 5: Invalidate Caches

**В Rider:**

1. `File → Invalidate Caches...`
2. Отметьте все опции:
   - ✅ Clear file system cache and Local History
   - ✅ Clear VCS Log caches and indexes
   - ✅ Clear downloaded shared indexes
3. Нажмите **Invalidate and Restart**
4. Дождитесь перезапуска и переиндексации

---

### Решение 6: Проверить Excluded Folders

Убедитесь, что `Docs/` не в excluded:

**В Rider:**

1. `File → Settings → Project → Directories`
2. Проверьте, что папка `Docs` НЕ отмечена как **Excluded**
3. Если отмечена → ПКМ → **Remove Exclusion**

---

### Решение 7: Открыть файлы напрямую

**Временное решение:**

1. В Rider: `Ctrl+Shift+N` (Navigate → File)
2. Введите имя файла: `README.md`
3. Если файл найден → откройте его
4. Если не найден → используйте `File → Open...` и откройте напрямую:
   ```
   /home/user/SuspenseCore/README.md
   /home/user/SuspenseCore/Docs/Changelog.md
   ```

---

### Решение 8: Полная переиндексация

**Если ничего не помогло:**

```bash
# 1. Закройте Rider

# 2. В терминале выполните:
cd /home/user/SuspenseCore
rm -rf .idea/  # Удалит настройки проекта Rider
git status     # Убедитесь, что файлы на месте

# 3. Откройте проект в Rider заново
# 4. Дождитесь полной индексации
```

**⚠️ Внимание:** Это удалит настройки проекта Rider (run configurations, etc.)

---

## 📋 Быстрая проверка

Выполните в терминале внутри проекта:

```bash
# Запустить скрипт проверки
./open-docs.sh

# Или вручную проверить:
git ls-files | grep README
ls -la README.md
cat README.md | head -5
```

**Ожидаемый результат:**
- Файлы присутствуют в git
- Файлы физически на диске
- Содержимое корректно

---

## 🐛 Если проблема сохраняется

### Проверить права доступа

```bash
# Права должны быть 644 для файлов
ls -la README.md

# Если права некорректны:
chmod 644 README.md .editorconfig
chmod -R 644 Docs/**/*.md
chmod -R 755 Docs/
```

### Проверить владельца файлов

```bash
# Файлы должны быть доступны текущему пользователю
ls -la README.md

# Если нужно изменить владельца:
# sudo chown -R $USER:$USER /home/user/SuspenseCore/
```

---

## 📞 Дополнительная помощь

Если ни одно решение не помогло:

1. **Проверьте версию Rider**: Убедитесь, что используете Rider 2023.3+
2. **Проверьте логи Rider**: `Help → Show Log in Explorer`
3. **Сообщите о проблеме**: Включите детали из `./open-docs.sh`

---

## ✅ Решение найдено?

После успешного решения проблемы можно удалить этот файл:

```bash
rm RIDER_TROUBLESHOOTING.md
```

---

**Последнее обновление:** 2025-11-23
